import { describe, expect, it } from 'vitest';

describe('Example tests', async () => {
  it('should be PASS test', async () => {
    const conditionalTest = true;

    expect(conditionalTest).toBeTruthy();
  });
});
